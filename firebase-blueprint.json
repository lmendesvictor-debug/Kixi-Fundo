{
  "entities": {
    "KixFundoState": {
      "title": "KixFundoState",
      "description": "Estojo completo do estado operacional e financeiro do Kixi-Fundo",
      "type": "object",
      "properties": {
        "currentMonth": {
          "type": "integer",
          "description": "Mês operacional corrente"
        },
        "payoutsCompleted": {
          "type": "object",
          "description": "Mapa dos pagamentos/contemplações mensais efetuadas"
        },
        "members": {
          "type": "array",
          "description": "Lista de cooperantes e suas respetivas quotas deduzidas"
        },
        "logs": {
          "type": "array",
          "description": "Audit trail de transações"
        },
        "loans": {
          "type": "array",
          "description": "Lista de contratos de apoio ao crédito concedidos (BI/NIF, fiador e cronograma de amortizações)"
        },
        "appConfig": {
          "type": "object",
          "description": "Parâmetros globais de configuração do Painel"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Instante do último sincronismo na cloud"
        },
        "carouselSlides": {
          "type": "array",
          "description": "Slides do carrossel sincronizados"
        },
        "customAdminPassword": {
          "type": "string",
          "description": "Palavra-passe de administrador personalizada"
        },
        "cloudFolderUrl": {
          "type": "string",
          "description": "URL da pasta de nuvem partilhada (Google Drive / OneDrive)"
        }
      },
      "required": [
        "currentMonth",
        "payoutsCompleted",
        "members",
        "logs",
        "appConfig"
      ]
    },
    "KixComprovativo": {
      "title": "KixComprovativo",
      "description": "Comprovativo de transferência bancária submetido pelos membros ou administradores",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "senderPhone": { "type": "string" },
        "fileName": { "type": "string" },
        "fileSize": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "detectedMemberName": { "type": "string" },
        "detectedMemberId": { "type": "integer" },
        "status": { "type": "string" },
        "targetMonth": { "type": "integer" },
        "uploadedBy": { "type": "string" },
        "fileDataUrl": { "type": "string" }
      },
      "required": ["id", "fileName", "timestamp", "targetMonth", "status"]
    }
  },
  "firestore": {
    "/kix_fundo/state": {
      "schema": "KixFundoState",
      "description": "Registro central persistentente fora do browser para o Kixi-Fundo"
    },
    "/kix_comprovativos/{comprovativoId}": {
      "schema": "KixComprovativo",
      "description": "Comprovativos individuais guardados na nuvem de forma descentralizada"
    }
  }
}
